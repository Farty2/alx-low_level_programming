#include <stdio.h>
#include "main.h"

/**
 * _strlen - returns the length of the passed string
 * @s: string to be supplied
 * Return: Length of spring
 */

int _strlen(char *s)
{
	int lenn = strlen(s);

	return (lenn);
}
